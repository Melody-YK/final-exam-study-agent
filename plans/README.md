# Implementation Plans

Generated by the improve skill on 2026-07-22 and extended on 2026-07-25. Plans 001-003 record the
completed delivery slices. Plans 004-008 are the authorized P0/P1 improvement backlog for the local
demonstration: connect ready sources to study actions, make the concept map and note templates
understandable, then add source verification and bounded invitation capacity. This is not an AIWF
stage plan or a production roadmap.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [001](001-query-conversation-continuity.md) | Isolate and deliver persistent query conversations | P1 | M | None | DONE: squash-merged to `main@dd239e9` on 2026-07-22 |
| [002](002-note-workflow-contract-capability.md) | Isolate note-workflow contracts and closed capability gates | P1 | M | 001 | DONE: squash-merged to `main@d8fe9d0` on 2026-07-23 |
| [003](003-note-workflow-data-foundation.md) | Land the demo-critical note persistence foundation | P1 | M | 002 | DONE: foundation included in `main@a67dc87` |
| [004](004-source-readiness-actions.md) | Turn document readiness into clear study actions | P0 | S | None | DONE: reconciled at `d593087` on 2026-07-25 |
| [005](005-actionable-concept-map.md) | Make the concept graph understandable and actionable | P0 | M | 004 | DONE: verified at `22e2774` on 2026-07-26 |
| [006](006-predictable-note-templates.md) | Make the three note templates predictable before generation | P0 | M | regeneration slice clean | DONE: reviewed at `59bfc5e` on 2026-07-26 |
| [007](007-source-preview-across-learning-surfaces.md) | Reuse original-page preview for notes and concept occurrences | P1 | L | 005, 006 | TODO after dependencies |
| [008](008-invitation-account-capacity.md) | Enforce an explicit invitation account capacity | P1 | M | None | TODO after P0 |
| [009](009-learning-loop.md) | 将可信课程资料闭环为主动回忆与复习队列 | P0 | L | 004, 005, 006 | DONE: baseline verified on 2026-08-02; title cleanup, draft restore, and learning-unit regeneration verified on 2026-08-03 |
| [010](010-conversation-core.md) | 将课程问答与单题问 AI 建成共享对话与教学内核 | P0 | XL | 009 | DONE: phases A-D and desktop/mobile acceptance verified on 2026-08-04 |

Status values: TODO | IN PROGRESS | REVISE (with reason) | DONE | BLOCKED (with reason) |
REJECTED (with rationale)

## Dependency notes

- Plan 001 has no code-plan dependency and must branch directly from verified `origin/main`.
- The former source-ref blocker is resolved: `codex/workflow-checkpoint@7c08333` is materialized,
  and the executor produced `codex/query-conversation-continuity@7f41817` from
  `origin/main@856cb1d` in a separate clean worktree.
- The first reconciliation requested bounded-context regression coverage. The executor amended
  only `services/api/tests/integration/test_queries.py`; the second reconciliation verified the
  turn, aggregate-character, per-field, ordering, and newest-context assertions and marked the
  plan `DONE` at `9682969`.
- Plan 001 subsequently passed the required GitHub check and was squash-merged as
  `main@dd239e91adcecc770c5c32d5dce754e73b2b2feb` (`feat: add persistent query conversations
  (#1)`). Its remote feature branch was deleted.
- Plan 002 depends on that main landing. Its source remains the mixed checkpoint at `7c08333`, but
  the checkpoint and new main are sibling histories with merge-base `856cb1d`; extraction must be
  symbol/hunk-aware and must preserve Plan 001's conversation contract.
- Plan 002 was implemented as local commit `44660ad`, independently reviewed, pushed through PR
  #2, and squash-merged as `main@d8fe9d0`. GitHub CI passed and the remote feature branch was
  deleted. The local `main` worktree is clean at that commit.
- Plan 003 branches directly from `main@d8fe9d0`. The mixed checkpoint remains its blob source,
  but the checkpoint's monolithic migration must be reduced to demo-critical task/version tables.
  The explicitly deferred export, cleanup, and `note-export` storage-purpose schema must not land.
- Plan 003 keeps migration `20260722_0008` because the already-landed P1 contract and the selected
  ORM shape include durable command/target and exact output-version fields. It exposes no new HTTP
  route and does not pull P3/P4 behavior forward.
- Plan 003 passed final reconciliation at local commit `f5cca9d`: focused PostgreSQL 18/18, full
  non-live Python 543/543, Web Vitest 40/40, Playwright 20/20, Alembic parity, generated-contract
  equality, mypy, Ruff, typecheck, lint, exact eight-file scope, and diff hygiene all passed. The
  branch remains local, clean, one commit above `main@d8fe9d0`, and has not been pushed or merged.
- Before a later slice writes multiple output versions for one Note, revise the 0008 downgrade
  path so restoring its former note-level uniqueness cannot fail on populated rows. This is a
  deferred activation risk, not a blocker for Plan 003's dormant tables.
- Plan 003's foundation is present in the later squash merge `main@a67dc87`; its retained local
  branch is historical and is not an execution dependency for Plans 004-008.
- Plan 004 is complete as one clean frontend-only commit at `d593087`. Focused/full Web tests,
  typecheck, lint, build, desktop/mobile Library E2E, exact four-file scope, and diff hygiene passed;
  contracts, dirty shared CSS, and the E2E mock were unchanged.
- Plan 005 is complete at `22e2774`: co-occurrence is undirected and explained, one-hop focus is
  reversible, and a selected concept opens a fresh unsent QA draft. Focused/full Web, static gates,
  screenshots, and 48 affected desktop/mobile E2E cases passed without backend changes.
- The note-regeneration prerequisite for Plan 006 was extracted unchanged from the primary dirty
  worktree, independently approved, and committed as `84e8d5c`. Plan 006 now executes from that
  clean baseline; the unrelated Demo Lab and shared-CSS changes remain only in the primary worktree.
- Plan 006 is complete at `59bfc5e`: the three existing styles expose predictive samples, all three
  have deterministic limits, complete truncation preserves whole-source/AST linkage, and exam focus
  omits redundant page headings. Backend, Web, E2E, static, scope, and contract-hash gates passed.
- Plan 007 follows 005 and 006 because it adds permission-scoped source actions to both surfaces. It
  must preserve query citation authorization and cannot fabricate read URLs in the browser.
- Plan 008 is independent in code but follows P0 in product order. It serializes active-seat and
  invitation reservation changes under PostgreSQL rather than claiming production scalability.

## P0 and P1 scope decision

- P0 is limited to existing product surfaces: source readiness and next actions, the deterministic
  concept map, and the three already-supported note styles. No new learning mode is introduced.
- P1 is limited to trustworthy original-page preview from notes/graph and a configurable active
  account/invitation capacity for the small invitation-only deployment.
- The plan treats OpenAPI JSON and generated TypeScript as regenerated outputs, not hand-picked
  checkpoint hunks.
- Plan 002 includes only dormant P1 Python contracts plus default-off config/error/capability
  behavior. Note migrations, ORM, historical versions, batch routes/services, runner, Notes UI,
  DOCX/export/cleanup, exact ETA, `.idea/`, and AIWF document edits are explicitly excluded.
- Plan 003 includes dormant PostgreSQL/ORM facts for note tasks and immutable versions only. It
  excludes export/cleanup tables, StoredObject purpose changes, repositories, state transitions,
  backfill, API/OpenAPI changes, Web code, and AIWF document edits.
- Plans 004-005 must not touch OpenAPI or generated TypeScript. Plan 006 executes on the reconciled
  regeneration commit `84e8d5c`; Plan 007 still waits for 005 and 006. Plan 008 may regenerate
  contracts only after rebasing and reviewing generated schema content.
- Explicitly deferred beyond P1: flashcards/active recall, spaced repetition, exam scheduling,
  audio/video, collaboration, a content marketplace, Neo4j, LLM relationship ontologies, external
  queue scaling, production LibreOffice isolation, exact ETA, DOCX/export, and observability work.

- Plan 009 is a user-requested product-direction draft for the first active-recall loop. It narrows
  V1 to source-bound multiple-choice and true/false practice, durable attempts, explainable mastery,
  and a bounded review queue. It does not replace the current graph algorithm or claim cloud
  capacity. Implementation must wait for the user to confirm the question types, mastery rules, runner boundary,
  and the decision to keep graph replacement as a separate plan.

## Findings considered and rejected

- Making the synthetic current query use an eligible historical status would strengthen mutation
  coverage, but it is not a Plan 001 blocker. The production call occurs after `start_retrieval()`
  sets the current query to `retrieving`, and the strict `(created_at, id)` predecessor predicate
  independently prevents the current row from entering its own context.
- Full active-recall/SRS work was considered but deferred: it is a new learning mode and should not
  compete with closing the existing source-to-study loop.
- Replacing the deterministic graph with Neo4j or LLM-inferred semantic edges was rejected for the
  local 2-core/2-GB demonstration; the current projection supports the planned P0 interactions.
